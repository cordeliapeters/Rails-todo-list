<h1>Sign Up!</h1>

<%= form_for @user, url: register_path do |u| %>
  <%= u.label :email %>
  <%= u.text_field :email %>

  <%= u.label :password %>
  <%= u.password_field :password %>

  <%= u.submit "Register" %>

<% end %>
